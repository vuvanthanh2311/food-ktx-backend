<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
</head>
<body>
    <div class="addProduct">
        <div class="addProduct__heading">
            <h1>Cập nhật cửa hàng</h1>
        </div>
        <div class="addProduct__form">
            <form class="form__editProduct" action="/admin/update-shop/{{shop._id}}?_method=PUT" enctype="multipart/form-data" method="POST">
                <div class="form__group">
                    <label for="name">Tên cửa hàng</label>
                    <input type="text" placeholder="Tên cửa hàng" name="name" id="name" value="{{shop.name}}">
                </div>
                <div class="form__group">
                    <label for="type">Phone</label>
                    <input type="text" placeholder="Loại cửa hàng" name="phone" id="phone" value="{{shop.phone}}">
                </div>
                <div class="form__group">
                    <label>Khoảng thời gian mở cửa</label>
                    <div class="form__group--price">
                        <input type="text" placeholder="Từ" name="timeOpen" id="timeOpen" value=" {{shop.timeOpen}}">
                        <input type="text" placeholder="Đến" name="timeClose" id="timeClose" value=" {{shop.timeClose}}">
                    </div>
                    
                </div>
                <div class="form__group">
                    <label for="type">Khu vực</label>
                    <input type="text" placeholder="khuA,khuB" name="zone" id="zone" value="{{shop.zone}}">
                </div>
                <div class="form__group">
                    <label for="type">Loại cửa hàng</label>
                    <input type="text" placeholder="store, coffee" name="type" id="type" value="{{shop.type}}">
                </div>
                <div class="form__group">
                    <label for="description">Mô tả</label>
                    <textarea type="text" placeholder="Mô tả cửa hàng" name="description" id="description" rows="4" cols="50" >{{shop.description}}</textarea>
                </div>

                <div class="form__group">
                    <label for="thumbnails">Hình ảnh</label>
                    <div class="Update_image_render">
                       
                            <img src="http://localhost:3000/open_image?image_name={{shop.thumbnails}}" alt="img">
                      
                          </div>
                        <input name="thumbnails" type="file" accept=".png,.jpg" multiple required>
                </div>  
               
            </form>
             <button  class="btn--primary btn-small btn-submit">SAVE</button>
            {{!-- <button class="btn--secondary btn-small btn-reset">Reset</button> --}}
        </div>
    </div>
</body>
<script>
    $(document).ready(function(){
        $(".btn-submit").click(function(){
            $(".form__editProduct").submit();
        })
    })
</script>
</html>